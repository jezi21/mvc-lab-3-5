<%- include('partials/navigations') %>
<h1>Authors</h1>
<% if (authors.length === 0) { %>
    <p>No authors has been found</p>
<% } else { %>
    <ul>
        <% authors.forEach(author => { %>
            <li><%= author.name %></li>
        <% }) %>
    </ul>
<% } %>